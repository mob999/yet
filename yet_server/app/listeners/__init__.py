from . import action_listeners as action_listeners


def register_listeners():
    # Just importing the module is enough because decorators run at import time.
    # But we can add explicit registration logic here if needed later.
    pass
